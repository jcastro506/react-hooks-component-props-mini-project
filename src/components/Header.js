import React from 'react'
import blogData from '../data/blog.js'

function Headers({ name }){
    return (
        <header>
            <h1>{name}</h1>
        </header>
    )
}

export default Headers 

